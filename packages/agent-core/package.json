{
  "name": "@ai-resume-agent/agent-core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --noEmit false --skipLibCheck true || true",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  }
}
