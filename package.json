{
  "name": "ai-resume-agent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/web && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
